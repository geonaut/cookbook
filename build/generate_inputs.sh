#!/usr/bin/env bash
set -euo pipefail

OUT="src/chapters/_auto_inputs.tex"
mkdir -p "$(dirname "$OUT")"

echo "% Auto-generated by build/generate_inputs.sh - DO NOT EDIT" > "$OUT"
echo "% Generated on $(date --rfc-3339=seconds 2>/dev/null || date)" >> "$OUT"
echo "" >> "$OUT"

# Find .tex files under src/chapters, exclude the auto file itself, sort for stable order.
# Use forward slashes and strip the leading "src/" so \input{chapters/...} works.
find src/chapters -type f -name '*.tex' ! -name '_auto_inputs.tex' | sort | while read -r f; do
  rel="${f#src/}"
  printf "%% included: %s\n" "$rel" >> "$OUT"
  printf "\\input{%s}\n\n" "$rel" >> "$OUT"
done

echo "Wrote $OUT"
